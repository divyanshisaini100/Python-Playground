def count_characters(text):
    char_count = {}
    for char in text:
        if char != ' ':
            char_count[char] = char_count.get(char, 0) + 1
    return char_count
# counts no of times a char occurs and excludes spaces
